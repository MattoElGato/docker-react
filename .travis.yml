language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t mattoelgato/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true mattoelgato/docker-react npm run test

